---
type: "agent_requested"
description: "UI Planing and implementation"
---
When asked to use shadcn componments, use the MCP server.

Planing Rule
Wgen asked to plan using anything related to shadcn
Use the MCP Server during planning
Apply components wherever components are applicable
Use whole blocks where possible (e.g, login page, calendar)

Implementation Rule
When implementing:

First call the demo tool to see how it is used
Then implement it so that it is imlplemented correctly.